management:
  endpoint:
    chaosmonkey:
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'
chaos:
  monkey:
    enabled: false
    watcher:
      restController: true
      service: false
    assaults:
      level: 3
      latency-active: true
spring:
  profiles:
    active: chaos-monkey
  cloud:
    kubernetes:
      enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

---
spring:
  config:
    activate:
      on-cloud-platform: kubernetes

eureka:
  client:
    enabled: false
